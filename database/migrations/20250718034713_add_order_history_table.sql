-- +goose Up
-- SQL in this section is executed when the migration is applied.
-- Эта миграция больше не нужна, так как все изменения уже были сделаны
-- в начальной миграции по созданию таблицы order_history.
-- Оставляем её пустой.

-- +goose Down
-- SQL in this section is executed when the migration is rolled back.